<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:label="RobloxMod"
        android:icon="@android:drawable/sym_def_app_icon"
        android:extractNativeLibs="true">

        <!-- Xposed / LSPosed / LSPatch discovery metadata -->
        <meta-data android:name="xposedmodule"       android:value="true"/>
        <meta-data android:name="xposedminversion"   android:value="82"/>
        <meta-data android:name="xposeddescription"  android:value="RobloxMod — Roblox client mod + executor"/>
        <meta-data android:name="xposedscope"        android:resource="@array/target_scope"/>

        <activity android:name=".LauncherActivity"
                  android:exported="true"
                  android:label="RobloxMod">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
